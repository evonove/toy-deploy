version: "2"

services:
  postgres:
    extends:
      file: ./containers/base.yml
      service: postgres

  redis:
    extends:
      file: ./containers/base.yml
      service: redis

  django:
    extends:
      file: ./containers/base.yml
      service: django
